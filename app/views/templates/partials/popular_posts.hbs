{{#if popular_posts}}
  <h1>Popular Posts</h1>
  <ul id="popular_posts">
    {{#each popular_posts}}
      <li class="post">
        <a href="{{root_url}}{{url}}">{{title}}</a>
      </li>
    {{/each}}
  </ul>
{{/if}}